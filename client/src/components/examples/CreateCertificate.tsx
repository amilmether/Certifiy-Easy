import { ThemeProvider } from "../ThemeProvider";
import CreateCertificate from "../../pages/CreateCertificate";

export default function CreateCertificateExample() {
  return (
    <ThemeProvider>
      <CreateCertificate />
    </ThemeProvider>
  );
}
